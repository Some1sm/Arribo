PORT=3000
DATA_DIR=
DB_PATH=
SNAPSHOT_RETENTION_HOURS=2
DELAY_RETENTION_DAYS=30
VEHICLE_SNAPSHOT_INTERVAL_MS=60000
AMB_API_KEY=

# Avanza SIRI account. Both are public well-known client defaults; leave blank
# to keep the built-in fallback. Set only to point at a different SIRI account.
MATARO_SIRI_ACCOUNT_ID=
MATARO_SIRI_ACCOUNT_KEY=

# Existing ORS origin (including /ors prefix if required), no trailing /v2
ORS_BASE_URL=
ORS_API_KEY=
